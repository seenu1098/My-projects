import { ZonefilterPipe } from './zonefilter.pipe';

describe('ZonefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ZonefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
